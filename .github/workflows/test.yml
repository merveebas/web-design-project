name: PR Yardımcısı

on:
  # Sadece "main" dalını hedefleyen pull request'ler açıldığında tetiklenir
  pull_request:
    branches: [ "main" ]
    types: [opened]

permissions:
  pull-requests: write # Pull request'e yorum yazma izni

jobs:
  post-comment:
    runs-on: ubuntu-latest
    steps:
      - name: PR'a karşılama yorumu ekle
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: 'Merhaba! 👋 Pull request için teşekkürler. Kodlarını en kısa sürede inceleyeceğiz. ✨'
            })

